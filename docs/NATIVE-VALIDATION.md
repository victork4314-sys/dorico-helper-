# Native validation

This branch triggers the pull-request macOS build so the complete SwiftUI, GameController, CoreHaptics, CoreMIDI, AppKit, and Accessibility target can be inspected through the connected GitHub tooling before the validation note is merged.
